---
context: default
phase: 7.3-e2e-consistency-validation (milestone v7.0 CLOSED)
task: milestone boundary — next milestone not yet chosen
status: paused_clean
last_updated: 2026-07-12T21:05:00+03:00
---

# Continue Here — NAOR (paused at v7.0 milestone boundary)

## Critical Anti-Patterns (do NOT repeat these)

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Trusting agent "verified" claims | v4.0 (Gemini-era) marked a 100% mock pipeline as "verified end-to-end"; audits passed on file-existence checks | blocking | Orchestrator independently re-runs commands / inspects pixels before every commit; audits must check CONTENT (real generation), not existence |
| Critical files outside git | MoMask helper lived only in the (gitignored, later deleted) toolchain dir and was lost in the wipe | blocking | Canonical copies live in-repo (AIPipeline/src/momask_helper/); setup_ai_tools.sh deploys them; anything outside git must be script-recreatable |
| Viewer-based alpha judgement | Image viewers ignored the alpha channel and made correct cutouts look broken | advisory | Verify transparency numerically (PIL alpha stats), not by eyeballing a render |

<current_state>
Milestone v7.0 "Consistency" closed and tagged (branch Efe-Basol, local-only, never pushed). Working tree clean, no servers running. The AI sprite pipeline is fully real and consistent-by-default: `python3 AIPipeline/generate_sprite.py "<motion prompt>"` → ~40 min → auto-sliced Unity sprite sheet, one consistent character, sword prop, stable facing. Identity flicker vs v6.0: -65% (hue/sat metric 0.2237→0.0780).
</current_state>

<completed_work>
- Milestone 6 "Pipeline Reality" (tag v6.0): replaced ALL mock modules with real, measured implementations (MoMask text→BVH ~7s; Blender beauty+depth render ~5s + hand-bone sword prop; ComfyUI SDXL+ControlNet-Depth ~140s/frame; Pillow packer + JSON sidecar + GIF preview; real Unity AutoSpriteImporter sidecar slicing; orchestrator with --skip-to resume). Unity UAT passed (16 slices).
- Milestone 7 "Consistency" (tag v7.0): IPAdapter hero-frame two-pass conditioning (default; --reference accepts a curated character image), --lock-facing root-yaw cancellation (default ON). Full E2E re-run validated (-65% flicker, back-view frames gone).
- 7.3b: toolchain wiped mid-run (user manual deletion) → recovered + hardened: idempotent AIPipeline/setup_ai_tools.sh provisions everything into NAOR_AI_TOOLS_DIR (default game_NAOR/AI_Tools, gitignored).
</completed_work>

<remaining_work>
- Next milestone decision (user): (a) back to game milestones — v1.0 refactor backlog / v2.0 UI / v3.0 scenes+story, with the pipeline feeding assets; or (b) art-quality round — curated per-character --reference design, better proxy-body anatomy, AnimateDiff-class shading smoothness.
- Optional: push Efe-Basol + tags to remote (do NOT push without the user asking).
- Cosmetic: user look at the updated sheet in Unity (importer already UAT'd; format unchanged).
</remaining_work>

<decisions_made>
See .planning/HANDOFF.json `decisions` — key ones: Fable-orchestrates/Sonnet-codes with independent verification; toolchain placement + setup script; consistent mode as default; sword as geometry, not prompt.
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/STATE.md` — current position + decisions
2. `.planning/ROADMAP.md` — milestone history (v4 mock warning, v6, v7) + backlog (v1/v2/v3)
3. `.planning/phases/7.3/SUMMARY.md` — latest phase incl. recovery details
4. `.planning/v7.0-MILESTONE-AUDIT.md` — what's proven vs carried forward

## Infrastructure State
- AI toolchain: `game_NAOR/AI_Tools/` (ComfyUI + IPAdapter node + ~12GB models, MoMask + checkpoints), gitignored; rebuild anytime with `AIPipeline/setup_ai_tools.sh`. Nothing running.
- Blender 5.1.2 at /Applications/Blender.app; pipeline utils venv at AIPipeline/.venv (gitignored).
- git: branch Efe-Basol, clean, tags v4.0/v6.0/v7.0, remote never pushed.

<context>
Working rhythm this session: user delegates ("sen yönet"), Fable plans/verifies/commits, Sonnet subagents implement per phase with explicit no-commit rules. Session limits occasionally kill subagents mid-run — resume them via SendMessage or salvage outputs and continue directly (temp outputs under AIPipeline/temp/<run_name>/ + --skip-to make resumes cheap).
</context>

<next_action>
Start with: read STATE.md/ROADMAP.md, ask the user to choose the next milestone direction (game features vs art quality), then /gsd-new-milestone.
</next_action>
