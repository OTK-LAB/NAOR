# Phase 2.1 Context: Main Menu Design & Implementation

## Goal
Design and implement a better, cleaner Main Menu script (`MainMenuController.cs`). It must be ready to attach to the `MainMenuScene.unity` and hook up to basic game transitions and settings smoothly.

## Status
- Current menu only has an `OptionsMenuScript.cs`.
- A robust, unified menu controller is missing.

## Next Steps
- Write `MainMenuController.cs` providing `Play`, `Options`, `Quit` functionality with smooth fade-ins and minimal boilerplate.
