# UAT Testing: Phase 5.3

## Scenario 1: Verify MoMask Environment
**Test Steps:**
1. Open a terminal.
2. Run `ls -ld /Volumes/aebasol_1tb/Ob/AI_Tools/MoMask/venv`

**Expected Result:** 
- The terminal outputs the directory details, confirming the virtual environment was created successfully.

**Status:** PASS

---

## Scenario 2: Verify Checkpoints
**Test Steps:**
1. Open a terminal.
2. Run `ls -lh /Volumes/aebasol_1tb/Ob/AI_Tools/MoMask/checkpoints/t2m/` (or check inside the `kit` folder).

**Expected Result:**
- Pre-trained weights (such as `.pt` or `.tar` files) exist inside the folder and have sizes over a few megabytes.

**Status:** PASS
