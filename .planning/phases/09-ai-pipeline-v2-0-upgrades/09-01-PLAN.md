---
version: 1.0
wave: 1
depends_on: []
files_modified:
  - AIPipeline/generate_sprite.py
  - AIPipeline/src/comfy_client.py
  - AIPipeline/src/blender_render.py
  - AIPipeline/src/color_consistency.py
  - AIPipeline/src/metrics/hue_sat_metric.py
  - AIPipeline/requirements.txt
  - Assets/Scripts/Editor/AutoSpriteImporter.cs
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - REQ-09-01
  - REQ-09-02
  - REQ-09-03
  - REQ-09-04
---

# Phase 09.1 Plan: AI Pipeline v2.0 Upgrades

<threat_model>
ASVS Level: 1
Block On: High
Threats:
- V5 Input Validation: Path traversal via character name derived reference-file lookup (crafted prompt text used to build a filesystem path).
Mitigation: Reuse `slugify()`'s existing `[a-z0-9_]`-only sanitization before any path construction; never string-concatenate raw prompt text into a filesystem path.
- V5 Input Validation: Malformed/corrupted curated reference image crashing the ComfyUI subprocess mid-run.
Mitigation: Mirror the existing `assert_image_not_degenerate()` guard pattern in `comfy_client.py` — validate the reference image loads and has non-trivial pixel variance before uploading it to ComfyUI.
</threat_model>

## must_haves
```yaml
must_haves:
  truths:
    - "Every generated sprite sheet with default flags has post-hoc color-matching applied"
    - "Blender proxy mesh geometry uses proper anatomical tapering based on BONE_RULES refinement"
    - "Character prompt matches against AIPipeline/character_refs/<slug>.png using slugify sanitization"
    - "TextureImporter.spritesheet deprecated API is replaced with ISpriteEditorDataProvider"
    - "hue_sat_metric.py exists in AIPipeline/src/metrics/ and can be executed"
    - "color-matcher is installed or pending verification before installation"
    - "D-01: Phase 9 targets the art-quality/consistency backlog, not integration UX. 'Kalite ve consistency' is the user's stated goal."
    - "D-02: Local ComfyUI on MPS, port 8188, venv Python — already running and integrated; no changes to execution environment."
    - "D-03: Python pipeline runs locally via venv on the Mac mini (Apple Silicon, 24GB unified memory). Do not schedule MoMask and SDXL concurrently."
    - "D-04: AI tools live in-repo at AI_Tools/ (gitignored), provisioned by AIPipeline/setup_ai_tools.sh; override via NAOR_AI_TOOLS_DIR. Anything outside git must be recreatable by the setup script."
  prohibitions:
    - statement: "AnimateDiff/HotshotXL-class temporal-consistency models must NOT be adopted"
      status: resolved
      verification: "Review pipeline imports and dependencies; no AnimateDiff or HotshotXL nodes or models are loaded."
    - statement: "Do NOT call assetImporter.SaveAndReimport() inside OnPreprocessTexture"
      status: resolved
      verification: "Review AutoSpriteImporter.cs to ensure SaveAndReimport is not called in OnPreprocessTexture."
```

## Artifacts this phase produces
- `AIPipeline/src/metrics/hue_sat_metric.py` (file path, promoted)
- `AIPipeline/character_refs/` (directory path)
- `AIPipeline/character_refs/.gitkeep` (file path)
- `AIPipeline/src/color_consistency.py` (new script path)
- `color_match_frame` (function in `AIPipeline/src/color_consistency.py`)
- `resolve_character_reference` (function in `AIPipeline/generate_sprite.py`)
- `--character` (CLI flag in `AIPipeline/generate_sprite.py`)
- `--no-color-match` (CLI flag in `AIPipeline/generate_sprite.py`)

## Tasks

### Wave 0: Setup, Metrics, and Verification Checkpoints

<task>
<id>09.1.01</id>
<title>Promote Metric and Prep Env</title>
<read_first>
- AIPipeline/temp/hue_sat_metric.py (if exists, else read 09-PATTERNS.md)
</read_first>
<action>
1. Move `AIPipeline/temp/hue_sat_metric.py` to `AIPipeline/src/metrics/hue_sat_metric.py`. If it doesn't exist in temp, create it verbatim using the code in `09-PATTERNS.md`.
2. Create directory `AIPipeline/character_refs/` and add a `.gitkeep` file to it.
</action>
<acceptance_criteria>
- `AIPipeline/src/metrics/hue_sat_metric.py` exists on disk.
- `AIPipeline/character_refs/.gitkeep` exists on disk.
</acceptance_criteria>
</task>

<task>
<id>09.1.02</id>
<title>checkpoint:human-verify dependency legitimacy</title>
<read_first>
- AIPipeline/requirements.txt
</read_first>
<action>
Review `color-matcher` package on PyPI/GitHub and the `ISpriteEditorDataProvider` API snippet for legitimacy. Wait for human confirmation. If approved, append `color-matcher` to `AIPipeline/requirements.txt` and run `AIPipeline/.venv/bin/python -m pip install color-matcher`.
</action>
<acceptance_criteria>
- Human acknowledges safety of `color-matcher` package and authorizes continuation.
- If approved, `AIPipeline/requirements.txt` contains the exact string `color-matcher`.
</acceptance_criteria>
</task>

### Wave 1: Quality and Consistency Fixes

<task>
<id>09.1.03</id>
<title>Implement Curated Character References</title>
<read_first>
- AIPipeline/generate_sprite.py
- AIPipeline/src/comfy_client.py
</read_first>
<action>
1. In `AIPipeline/generate_sprite.py`, implement `resolve_character_reference(prompt: str, explicit_ref: str | None) -> str | None`. It must use `slugify(prompt)` and check `os.listdir` on `CHARACTER_REFS_DIR` for a matching `.png` file.
2. Add CLI flag `--character` to allow explicit reference override in `generate_sprite.py`.
3. Wire the resolved reference into `args.reference` so it's passed to `comfy_client.py` as `--reference`.
4. In `AIPipeline/src/comfy_client.py`, add a call to `assert_image_not_degenerate` on the loaded `reference_image_path` before `upload_image`.
</action>
<acceptance_criteria>
- `AIPipeline/generate_sprite.py` contains `def resolve_character_reference(`.
- Running `python3 AIPipeline/generate_sprite.py "hero test" -h` shows the `--character` flag.
- `AIPipeline/src/comfy_client.py` contains `assert_image_not_degenerate` applied to the reference image flow.
</acceptance_criteria>
</task>

<task>
<id>09.1.04</id>
<title>Refine Proxy Mesh Anatomy</title>
<read_first>
- AIPipeline/src/blender_render.py
</read_first>
<action>
In `AIPipeline/src/blender_render.py`, modify `BONE_RULES` and the `add_capsule` logic to implement proper anatomical tapering. Adjust `radius1` and `radius2` based on bone classification instead of uniform cylinders to fix limb elongation. Update `bone_radius` to return two values if necessary.
</action>
<acceptance_criteria>
- `AIPipeline/src/blender_render.py` contains `radius1` and `radius2` logic inside `add_capsule`.
- Code correctly executes without Blender API errors when generating a proxy mesh.
</acceptance_criteria>
</task>

<task>
<id>09.1.05</id>
<title>Implement Post-Hoc LAB Color Matching</title>
<read_first>
- AIPipeline/generate_sprite.py
</read_first>
<action>
1. Create `AIPipeline/src/color_consistency.py` implementing `color_match_frame`. If `color-matcher` was approved, use `ColorMatcher` and mask on `alpha > 10` exactly as defined in `09-PATTERNS.md`. If rejected, implement the fallback Pillow-only `Image.quantize()` logic required by `09-RESEARCH.md` (quantize the hero/reference frame to a shared palette, re-quantize every other frame to that same palette). Implement a CLI entry point for processing a directory of frames.
2. In `AIPipeline/generate_sprite.py`, add `"colormatch"` to `STAGES` between `"stylize"` and `"pack"`.
3. Add `--no-color-match` CLI flag in `generate_sprite.py` (action="store_true", default=False).
4. Add the stage block for `"colormatch"` invoking `color_consistency.py` as a subprocess when `skip_idx <= STAGES.index("colormatch")`.
</action>
<acceptance_criteria>
- `AIPipeline/src/color_consistency.py` exists with conditional/fallback logic for `color-matcher`.
- `AIPipeline/generate_sprite.py` contains `"colormatch"` in the `STAGES` list.
- `generate_sprite.py` `-h` output lists `--no-color-match`.
</acceptance_criteria>
</task>

<task>
<id>09.1.06</id>
<title>Modernize Unity AutoSpriteImporter</title>
<read_first>
- Assets/Scripts/Editor/AutoSpriteImporter.cs
</read_first>
<action>
In `Assets/Scripts/Editor/AutoSpriteImporter.cs`, replace the `#pragma warning disable CS0618` block configuring `importer.spritesheet` with the `ISpriteEditorDataProvider` API. Build `SpriteRect` objects, set name/file IDs using `ISpriteNameFileIdDataProvider`, and call `dataProvider.Apply()`. Do NOT call `SaveAndReimport()`.
</action>
<acceptance_criteria>
- `Assets/Scripts/Editor/AutoSpriteImporter.cs` does not contain `TextureImporter.spritesheet`.
- `Assets/Scripts/Editor/AutoSpriteImporter.cs` contains `factory.GetSpriteEditorDataProviderFromObject`.
- No `SaveAndReimport` is called inside `OnPreprocessTexture`.
</acceptance_criteria>
</task>

<task>
<id>09.1.07</id>
<title>Persist Requirements</title>
<read_first>
- .planning/REQUIREMENTS.md
</read_first>
<action>
Append the new requirements `REQ-09-01` through `REQ-09-04` to `.planning/REQUIREMENTS.md` to officially track them.
</action>
<acceptance_criteria>
- `.planning/REQUIREMENTS.md` contains `REQ-09-01`, `REQ-09-02`, `REQ-09-03`, and `REQ-09-04`.
</acceptance_criteria>
</task>
