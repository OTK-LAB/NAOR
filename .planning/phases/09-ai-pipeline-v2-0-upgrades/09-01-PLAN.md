---
phase: 09-ai-pipeline-v2-0-upgrades
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AIPipeline/src/metrics/hue_sat_metric.py
  - AIPipeline/character_refs/README.md
autonomous: true
requirements: [AIQ-05, AIQ-01]
must_haves:
  truths:
    - "The identity-flicker metric can be run from a git-tracked location and reproduces the v7.0 baseline (~0.0780) on the existing heavy_sword_swing sheet."
    - "A git-tracked, non-ignored directory exists to hold curated per-character IPAdapter reference images."
  artifacts:
    - AIPipeline/src/metrics/hue_sat_metric.py
    - AIPipeline/character_refs/README.md
  key_links:
    - "hue_sat_metric.py is invocable as `AIPipeline/.venv/bin/python AIPipeline/src/metrics/hue_sat_metric.py <sheet.png>` and prints a mean pairwise hue/sat distance."
    - "git status shows AIPipeline/character_refs/README.md as tracked (NOT matched by any .gitignore rule)."
---

<objective>
Promote the identity-flicker measurement tool out of the gitignored working directory and stand up the git-tracked curated-reference directory. This is the measurement + scaffolding foundation every downstream quality plan compares against.

Purpose: Phase 9 must measure every quality change against the v7.0 identity-flicker baseline (hue/sat 0.0780). The baseline tool (`hue_sat_metric.py`) currently lives untracked in `AIPipeline/temp/` (gitignored) and would be lost on a clean checkout (RESEARCH Pitfall 6). Curated reference art (RESEARCH Pitfall 4) must live in a git-tracked directory, never under `temp/` or `AI_Tools/`.
Output: A git-tracked `AIPipeline/src/metrics/hue_sat_metric.py` and a git-tracked `AIPipeline/character_refs/` directory with a convention-documenting README.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ai-pipeline-v2-0-upgrades/09-RESEARCH.md
@.planning/phases/09-ai-pipeline-v2-0-upgrades/09-PATTERNS.md
@AIPipeline/temp/hue_sat_metric.py
</context>

## Artifacts this phase produces

Symbols/paths created by THIS plan (new — exclude from source-drift verification):
- `AIPipeline/src/metrics/hue_sat_metric.py` (new file path — verbatim promotion of the existing temp script; functions `cell_mean_hs`, `hue_dist`, `main` moved unchanged)
- `AIPipeline/character_refs/` (new git-tracked directory)
- `AIPipeline/character_refs/README.md` (new file — reference-library naming convention doc)

<tasks>

<task type="auto">
  <name>Task 1: Promote hue_sat_metric.py into a git-tracked metrics package</name>
  <files>AIPipeline/src/metrics/hue_sat_metric.py</files>
  <read_first>
    - AIPipeline/temp/hue_sat_metric.py (the exact 65-line source of truth to promote verbatim — copy its content unchanged)
    - AIPipeline/src/pack_sprites.py (sibling script-in-src convention: standalone `python3 script.py` scripts under AIPipeline/src/, no package __init__ required)
  </read_first>
  <action>
    Create `AIPipeline/src/metrics/hue_sat_metric.py` containing the byte-for-byte content of `AIPipeline/temp/hue_sat_metric.py` (functions `cell_mean_hs`, `hue_dist`, `main`, and the `if __name__ == "__main__"` guard). Do NOT rewrite the pairwise-distance algorithm, the `alpha > 10` figure-pixel mask, or the 4x4 cell grid — this is a promotion, not a refactor. Do NOT add an `__init__.py` (the script is invoked directly, not imported; matches how sibling scripts under AIPipeline/src/ are run). Leave the original `AIPipeline/temp/hue_sat_metric.py` in place (it is gitignored and harmless); the authoritative copy is now the tracked one. `git add AIPipeline/src/metrics/hue_sat_metric.py` so it is staged for commit.
  </action>
  <verify>
    <automated>AIPipeline/.venv/bin/python AIPipeline/src/metrics/hue_sat_metric.py Assets/Art/Sprites/Generated/heavy_sword_swing_Sheet.png | grep -q "mean pairwise hue/sat distance: 0.0779" && test -z "$(git check-ignore AIPipeline/src/metrics/hue_sat_metric.py)"</automated>
  </verify>
  <acceptance_criteria>
    - Running `AIPipeline/.venv/bin/python AIPipeline/src/metrics/hue_sat_metric.py Assets/Art/Sprites/Generated/heavy_sword_swing_Sheet.png` prints a line matching `mean pairwise hue/sat distance: 0.0779...` (reproduces the v7.0 baseline of ~0.0780, confirming the algorithm was moved unchanged).
    - `git check-ignore AIPipeline/src/metrics/hue_sat_metric.py` produces no output (the file is NOT gitignored).
    - The new file's function names are exactly `cell_mean_hs`, `hue_dist`, `main` (`grep -c "def cell_mean_hs" AIPipeline/src/metrics/hue_sat_metric.py` returns 1).
  </acceptance_criteria>
  <done>The baseline metric runs from a git-tracked path and reproduces ~0.0780 on the existing sheet.</done>
</task>

<task type="auto">
  <name>Task 2: Create the git-tracked curated-reference directory with a convention README</name>
  <files>AIPipeline/character_refs/README.md</files>
  <read_first>
    - .planning/phases/09-ai-pipeline-v2-0-upgrades/09-RESEARCH.md (Pitfall 4: the directory must be a git-tracked sibling of AIPipeline/src/, NOT under temp/ or AI_Tools/; Pattern 1: files are named `<slug>.png` where slug matches a character keyword found in the prompt via slugify())
    - AIPipeline/generate_sprite.py (slugify() at lines 48-52 — the sanitizer that will match filenames; document that filenames must be a slug of lowercase [a-z0-9_])
  </read_first>
  <action>
    Create `AIPipeline/character_refs/README.md` documenting the curated-reference-library convention: (1) each file is a hand-curated PNG named `<slug>.png` where `<slug>` is the output of `generate_sprite.py`'s `slugify()` for the character keyword expected in prompts (lowercase, `[a-z0-9_]` only); (2) resolution is name-based — `resolve_character_reference()` (added in plan 09-05) auto-selects the file whose stem appears in the prompt slug, else falls back to hero-frame auto-pick; (3) these files are hand-picked art and are NOT recreatable by `setup_ai_tools.sh`, so they MUST be committed to git — never placed under `AIPipeline/temp/` or `AI_Tools/` (both gitignored); (4) recommended reference resolution ~1024x1024 to match the IPAdapter conditioning size. `git add` the README so the directory is tracked. Note in the README that seeding the first real reference PNGs (e.g. a hand-touched best frame per named character) is a user curation step.
  </action>
  <verify>
    <automated>test -f AIPipeline/character_refs/README.md && test -z "$(git check-ignore AIPipeline/character_refs/README.md)" && grep -qi "slug" AIPipeline/character_refs/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `AIPipeline/character_refs/README.md` exists and is NOT gitignored (`git check-ignore` yields nothing).
    - The README states the `<slug>.png` naming convention and that files must be git-committed, not placed under temp/ or AI_Tools/.
    - `git status --short AIPipeline/character_refs/` shows the README as added/tracked, not ignored.
  </acceptance_criteria>
  <done>A git-tracked character_refs/ directory exists with a README documenting the naming + placement convention.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → git | Files added here are committed and travel to fresh clones; the only concern is that measurement/reference assets are NOT accidentally gitignored. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-INFRA | Repudiation | Untracked baseline tool lost on clean checkout | low | mitigate | Promote hue_sat_metric.py into git-tracked src/metrics/; verify via `git check-ignore` producing no output. |

Note: This plan introduces no prompt→filesystem-path resolution and no network surface — the path-traversal (T-09-01) and reference-image DoS (T-09-02) threats are introduced and mitigated in plan 09-05. No package installs occur in this plan (T-09-SC handled in plan 09-04). Local single-user offline tooling; ASVS L1 categories V2/V3/V4/V6 not applicable.
</threat_model>

<verification>
- `AIPipeline/.venv/bin/python AIPipeline/src/metrics/hue_sat_metric.py Assets/Art/Sprites/Generated/heavy_sword_swing_Sheet.png` prints ~0.0779 mean pairwise hue/sat distance.
- `git check-ignore AIPipeline/src/metrics/hue_sat_metric.py AIPipeline/character_refs/README.md` prints nothing (both tracked).
</verification>

<success_criteria>
- Baseline metric reproducible from a git-tracked path (~0.0780 on the existing sheet).
- character_refs/ exists as a tracked directory with a documented naming/placement convention.
</success_criteria>

<output>
Create `.planning/phases/09-ai-pipeline-v2-0-upgrades/09-01-SUMMARY.md` when done.
</output>
