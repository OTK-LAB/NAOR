---
context: phase
phase: 09-ai-pipeline-v2-0-upgrades
task: planning (interrupted)
total_tasks: n/a
status: in_progress
last_updated: 2026-07-13T08:25:30.135Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Stale-premise planning | An earlier Gemini/Antigravity discuss session scoped Phase 9 as "replace mock generation with real ComfyUI + Unity Editor window" — but the pipeline has been fully real since Milestone 6 (v7.0 shipped IPAdapter consistency). The draft produced cosmetic plans that missed the user's actual pain. | blocking | Read 09-CONTEXT.md (rewritten 2026-07-13) as the ONLY authoritative scope. Phase 9 = quality + consistency backlog. The Unity Editor trigger window is DEFERRED — do not plan or build it. |

<current_state>
Paused mid `/gsd-plan-phase 9`, at user request, while the planner subagent was writing plans.

Pipeline stage of the workflow when stopped:
- Research: DONE (09-RESEARCH.md, commit f9b38457)
- Validation template: DONE (09-VALIDATION.md, commit 16e0e787)
- Pattern mapping: DONE (09-PATTERNS.md)
- Planner (opus): KILLED mid-write — it intended SIX plans, wrote only `09-01-PLAN.md` (Wave 1: promote hue_sat_metric.py to `AIPipeline/src/metrics/`, scaffold git-tracked `AIPipeline/character_refs/`). Plans 02–06 were never written.
- Plan-checker: NEVER RAN. No coverage gates ran. STATE.md not updated to "Ready to execute".
</current_state>

<completed_work>

- 09-CONTEXT.md rewritten: scope corrected to art-quality/consistency backlog (palette drift via curated IPAdapter refs, capsule-proxy anatomy, shading smoothness, AutoSpriteImporter API modernization). ROADMAP.md Phase 9 goal updated to match.
- gsd-phase-researcher: 09-RESEARCH.md — AnimateDiff ruled out (per-frame loop incompatibility + 24GB VRAM risk); LAB color-match pass recommended instead (`color-matcher` PyPI, [SUS]-flagged → needs checkpoint:human-verify; Pillow-quantize fallback); anatomy root cause is Blender-tier (`build_body_mesh()` tapering); Unity fix is ISpriteEditorDataProvider migration.
- gsd-pattern-mapper: 09-PATTERNS.md — `resolve_prop()`/`slugify()` is the template for `resolve_character_reference()`; new Stage 3.5 `colormatch` slots into the `STAGES`/`run_stage()` structure; `alpha > 10` masking convention must be reused in new `color_consistency.py`.
- Planner verified before dying: baseline metric reproduces as **0.077992** on existing heavy_sword_swing sheet; cached frames exist in `AIPipeline/temp/`; `color-matcher` NOT installed in venv.
</completed_work>

<remaining_work>

- Plans 02–06: never written (color-match stage, character_refs auto-selection, Blender anatomy + diagnostic, AutoSpriteImporter migration, E2E measurement/sign-off — exact split unknown, planner died before writing them).
- Plan-checker verification loop (max 3 iterations).
- Requirements coverage gate — NOTE: 09-01-PLAN.md cites `AIQ-01`/`AIQ-05`; ROADMAP says `Requirements: TBD`. Verify these IDs exist in REQUIREMENTS.md or fix the frontmatter.
- state.planned-phase, roadmap.annotate-dependencies, final plan commit.
</remaining_work>

<decisions_made>

- Phase 9 targets quality + consistency ONLY (user's explicit call after rejecting the mis-scoped draft). Unity Editor window deferred.
- Shading smoothness = post-hoc LAB color-match (default ON, `--no-color-match` escape hatch per existing `--no-X` convention), NOT AnimateDiff.
- Anatomy = incremental `BONE_RULES` tapering in `blender_render.py` first, gated by a diagnostic that inspects raw beauty renders pre-SDXL.
- Reference library = git-tracked `AIPipeline/character_refs/` (NOT gitignored `temp/`/`AI_Tools/`), name-based auto-selection modeled on `resolve_prop()`.
- Budget guard: consistent-mode ~150s/frame must not grow without explicit sign-off; improvements measured against 0.0780 baseline.
</decisions_made>

<blockers>
- None. Pause was a user request, not a failure.
</blockers>

## Required Reading (in order)
1. `.planning/phases/09-ai-pipeline-v2-0-upgrades/09-CONTEXT.md` — authoritative scope (rewritten; ignore any older draft premise)
2. `.planning/phases/09-ai-pipeline-v2-0-upgrades/09-RESEARCH.md` — technique decisions + [SUS] flags
3. `.planning/phases/09-ai-pipeline-v2-0-upgrades/09-PATTERNS.md` — code analogs the plans must follow
4. `.planning/phases/09-ai-pipeline-v2-0-upgrades/09-01-PLAN.md` — the one plan that exists (decide: keep via "Add more plans" or discard via "Replan from scratch")

## Infrastructure State
- No background processes running (planner agent stopped; ComfyUI not started this session).
- Working tree also carries pre-existing, unrelated changes: `Assets/Scripts/Prototype/*` deletions + STATE/ROADMAP edits from Phase 8 work. Do not mix them into Phase 9 commits.
- `color-matcher` not installed in `AIPipeline` venv (planner verified) — Wave 0/setup task must add it to requirements.txt.

<context>
Orchestration model: Fable manages/verifies, Sonnet/Opus subagents produce artifacts (researcher=sonnet, planner=opus, checker=sonnet per config). Security capability active: every PLAN.md needs a <threat_model> block (ASVS L1, block on high).
</context>

<next_action>
Start with: `/gsd-resume-work`, then `/gsd-plan-phase 9`. At step 6 it will detect 09-01-PLAN.md — "Replan from scratch" is recommended (only 1 of 6 plans exists; a fresh coherent run beats grafting five plans onto one orphan). Then let the plan-checker loop and coverage gates finish.
</next_action>
