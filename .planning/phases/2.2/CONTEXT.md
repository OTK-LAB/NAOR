# Phase 2.2 Context: Connect the menu to the game state and settings

## Goal
Revive and clean up the `OptionsMenuScript.cs`. It contains commented-out resolution dropdown logic that needs to be active so players can actually set their resolution.

## Plan
1. Uncomment the `Start()` method in `OptionsMenuScript.cs`.
2. Clean it up to align with Ponytail Ultra standards (remove redundant loops if possible, but keep it simple and native).
