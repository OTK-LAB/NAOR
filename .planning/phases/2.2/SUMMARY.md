# Phase 2.2 Summary

Phase 2.2 is complete. The `OptionsMenuScript.cs` was cleaned up by uncommenting the resolution dropdown system, connecting it effectively to Unity's native `Screen.resolutions`.
