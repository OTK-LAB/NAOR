# Milestone v6.0 "Pipeline Reality" — Audit

**Date:** 2026-07-12
**Verdict:** ✅ PASSED — all requirements met, E2E verified with real output, user UAT confirmed.

## Origin
2026-07-11 audit of v4.0 found all five pipeline modules were mocks (`time.sleep` stubs, text placeholders named .png, empty `workflows/`) despite being marked "verified end-to-end". Milestone 6 was opened to replace every mock with a real implementation.

## Requirements coverage
| Requirement | Status | Evidence |
|---|---|---|
| Real text→motion (MoMask, local, free) | ✅ | 6.1 — real 22-joint BVH in ~5-7s on MPS; verified non-degenerate motion (commit 6579e43) |
| Real Blender headless render | ✅ | 6.2/6.2b — connected capsule humanoid + hand-bone sword prop, beauty+depth @1024, ~0.3s/frame-pair (1adcbe4, 4933c91) |
| Real ComfyUI stylization | ✅ | 6.3 — SDXL + ControlNet-Depth, built-in nodes only, ~137s/frame MPS, alpha-cutout transparent sprites (516e44d) |
| Real packing + Unity ingestion | ✅ | 6.4 — Pillow grid packer + JSON sidecar + GIF preview + real orchestrator with --skip-to (039fdbe) |
| E2E: text prompt → Unity-ready sheet, zero manual steps | ✅ | 37.4-min verified run; sheet visually inspected by orchestrator |
| Unity auto-slicing | ✅ | User UAT 2026-07-12: 16 slices visible, no errors |

## Cross-phase integration
6.1→6.2 (real MoMask BVH into Blender): verified. 6.2→6.3 (depth/beauty into ControlNet + alpha cutout): verified, incl. sword-prop fix loop. 6.3→6.4 (stylized frames → sheet → Unity): verified.

## Honest quality notes (carried forward, not blockers)
1. Frame-to-frame character identity flicker — independent SDXL generations; fix candidates: IPAdapter, img2img chaining, character LoRA.
2. Occasional back-view frames (MoMask rotates character) — facing constraint needed.
3. Elongated proportions mirror the capsule proxy body.
4. `TextureImporter.spritesheet` deprecated (works on Unity 6000.5).

## Process note
Executed under the "Fable orchestrates, Sonnet subagents implement" model: every subagent report was independently re-verified by the orchestrator (re-running commands, pixel-level checks, visual inspection) before commit. One subagent claim ("cutout verified") was double-checked numerically after a misleading viewer render — claim held.
