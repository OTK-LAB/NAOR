# Milestone v7.0 "Consistency" — Audit

**Date:** 2026-07-12
**Verdict:** ✅ PASSED — both consistency mechanisms implemented, measured, and validated on a full E2E re-run.

## Requirements coverage
| Requirement | Status | Evidence |
|---|---|---|
| Character identity stable across frames | ✅ | IPAdapter hero-frame conditioning (7.1); full-sheet identity flicker **-65%** (0.2237→0.0780); visual: one knight vs M6's 3+ characters |
| Facing flips eliminated | ✅ | `--lock-facing` (7.2): root yaw drift 21°→0°; final sheet has no back-view frames (M6 had 2) |
| Orchestrator integration, consistent-by-default | ✅ | 7.3 wiring (e43ede4); `--no-lock-facing`/`--no-ipadapter` escape hatches |
| E2E validation vs M6 baseline | ✅ | Full 16-frame re-run (interrupted once by external toolchain wipe; resumed via salvage + `--reference`); baseline preserved at AIPipeline/temp/m6_baseline |

## Bonus (unplanned, 7.3b)
Disaster-recovery hardening after the user manually wiped the AI toolchain mid-run: committed idempotent `setup_ai_tools.sh`, toolchain relocated to `game_NAOR/AI_Tools` (gitignored, user's placement choice), lost MoMask helper recreated with canonical in-repo copy. Toolchain re-verified end-to-end post-recovery.

## Carried forward
- Unity-side re-check of the new sheet is cosmetic (format/path identical to the v6.0-UAT'd sheet).
- Palette drift within identity (accent tones) still possible; a curated per-character reference image via `--reference` is the intended end state.
- AnimateDiff-class temporal smoothing (sub-frame shading coherence) remains out of scope.
